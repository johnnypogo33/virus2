# Mongo DB access information.
# -----
# On first run, MONGO_USER and MONGO_PASS will be populated with UUIDs
# automatically; if you change these, the existing database will no longer
# be readable.
#
# export MONGO_USER=root
# export MONGO_PASS=some-complex-password
# DOCKERPORT initially populated by what's in ./config/versioned
# export DOCKERPORT=1234
#
# Environment usage
# -----
# Use "dev" to load development items; "prod" will not.
#
# export ENVIRONMENT_USAGE=dev
